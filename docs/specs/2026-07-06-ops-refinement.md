---
date: 2026-07-06
source: customer request (pasted into chat with matching screenshot)
status: received
note: verbatim customer wording, typos included — do not edit; interpretation lives in the brainstorm/requirements doc
---

# Customer spec — 2026-07-06 (verbatim)

In Admin view, Students page, Next to each student's name, highlight their real-time status (e.g. at school, on bus, dropped-off home, absent today). Status to be reset every day

Notifications on Parent view, to be moved from Recent to History after 24H. History keeps notofications for 7 days

In Admin view, Students page, when I create a new student do not give a default status "at-school". Status will show only once student is assigned to a route. Real time status will be displayed.

When new student is assigned to an existing route, Route stops order and pick up time should be recalculated

In Admin view, Routes page, in each route, changing the order of stops should be allowed in case of need (e.g. traffic jam, road works, etc)

In Parents view, introduce a Cancel-a-Ride feature letting parents submit ride cancellation requests for the same day. Admin will receive the notification, driver stops list list will update

In Admin view, allow admin to send a time sensitive messages to all parents of the students assigned to a specific route.
